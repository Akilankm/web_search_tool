# Offline Capture Separation Note

This temporary review note exists only to force a clean PR surface for the notebook-only offline capture cleanup.

The offline artifact workflow must remain separate from the main tournament flow:

```text
main notebooks: product discovery and champion confirmation only
offline notebook: optional offline page capture only
```

Final user-facing entrypoint:

```text
notebooks/03_offline_product_artifact.ipynb
```
